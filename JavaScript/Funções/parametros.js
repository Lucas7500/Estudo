// Uma função pode ter parâmetros, que precisarão ser definidos para o funcionamento da função

function sum(number1, number2) {
  total = number1 + number2
  return total
}

const resultado = sum(4, 5)
console.log(`O resultado é ${resultado}`)
